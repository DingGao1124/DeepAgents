interface:
  display_name: "Eva Lottery"
  short_description: "Generate an Eva lottery fragment with gift carousel, draw buttons, win modal, and winner list scroll."
  default_prompt: "Use $eva-lottery to generate a lottery fragment for the Eva activity page."
