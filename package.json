{
  "name": "web-page-automation-agent",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --recursive --parallel dev",
    "dev:backend": "pnpm --filter web-page-agent-backend dev",
    "dev:frontend": "pnpm --filter web-page-agent-frontend dev",
    "build": "pnpm --filter web-page-agent-frontend build",
    "typecheck": "pnpm --recursive typecheck",
    "setup": "pnpm install && (cd backend && cp -n .env.example .env 2>/dev/null || true)"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
